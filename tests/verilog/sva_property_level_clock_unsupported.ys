# A property clocking event must be edge triggered.
logger -expect error "An SVA property needs an edge-triggered clocking event" 1
read_verilog -formal -sv <<EOT
module top (input clk, input a, input b);
	assert property (@(clk) a |-> b);
endmodule
EOT
